# Generated cases

Run `python3 test/perf/solve_before/gen_cases.py` to materialize the deterministic
SMT2 files in this directory, or use `run_solve_before_perf.py`, which generates
them under `/tmp/bitwuzla-sb-perf/cases` by default.
